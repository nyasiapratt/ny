import java.util.ArrayList;
import java.util.Arrays;

class Starter{
public static void main(String[] args) {
/** get file path & call complier */
Complier(String filepath){
Lex lex = new Lex(lexumeString:"These are all the words in my file");
Syntax syntax = new(lex.tonenize())  
			String msg = "StudyTonight.com/tutorial/java/string";
		System.out.println(msg);
		
		ArrayList<String> list = new ArrayList<>(Arrays.asList(msg.split("/")));
		list.forEach(System.out::println);	
	}
}
   }
}